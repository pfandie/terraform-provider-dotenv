---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dotenv_file Ephemeral Resource - terraform-provider-dotenv"
subcategory: ""
description: |-
  Loads a .env file ephemerally — values are never persisted in the plan or state file. Requires Terraform >= 1.10.
---

# dotenv_file (Ephemeral Resource)

Loads a `.env` file ephemerally — values are never persisted in the plan or state file. Requires Terraform >= 1.10.

## Example Usage

```terraform
# Values are not persisted in the state file or plan
ephemeral "dotenv_file" "secrets" {
  env_path = "../../.."
  env_file = ".env.secrets"
}

resource "aws_secretsmanager_secret_version" "app" {
  secret_id                = aws_secretsmanager_secret.app.id
  secret_string_wo         = jsonencode(ephemeral.dotenv_file.secrets.values)
  secret_string_wo_version = 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `env_file` (String) Name of the env file inside `env_path`. Defaults to `.env`.
- `env_path` (String) Directory containing the env file. Defaults to the current working directory (`.`).
- `exclude_envs` (List of String)
- `include_empty` (Boolean)
- `include_envs` (List of String)
- `sensitive_envs` (List of String)

### Read-Only

- `sensitive_values` (Map of String, Sensitive)
- `values` (Map of String)
